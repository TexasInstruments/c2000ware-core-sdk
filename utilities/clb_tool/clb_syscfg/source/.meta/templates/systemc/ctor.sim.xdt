%%{
    let inst      = args[0];
    let name      = args[1];
    let inputs    = args[2];
    let Common    = system.getScript("/utilities/clb_tool/clb_syscfg/source/Common.js");
%%}
        `name`("`name`"),